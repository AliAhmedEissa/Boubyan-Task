package com.boubyan.core.base

/**
 * Base interface for all repositories in the application.
 * This interface serves as a contract for repository implementations,
 * ensuring consistent repository behavior across the app.
 */
interface BaseRepository